{
  "name": "septic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:app": "CAP_BUILD=1 vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build:app && cap sync",
    "ios": "npm run build:app && cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  }
}
